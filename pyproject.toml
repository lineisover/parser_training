[tool.ruff]
fix = true
line-length = 120

[tool.ruff.lint]
select = ["E", "F", "W", "I", "N", "B", "DJ", "Q"]

[tool.ruff.flake8-quotes]
inline-quotes = "single"

[tool.ruff.format]
quote-style = "single"
skip-magic-trailing-comma = false